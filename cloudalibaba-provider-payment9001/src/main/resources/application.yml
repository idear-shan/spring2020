server:
  port: 6001
spring:
  application:
    name: nacos-payment-provider #注册名称
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        #检查下面所有
management:
  endpoints:
    web:
      exposure:
        include: "*"